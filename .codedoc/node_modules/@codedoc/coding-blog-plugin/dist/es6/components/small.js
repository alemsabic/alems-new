import { themedStyle } from '@connectv/jss-theme';
export const SmallStyle = themedStyle(theme => ({
    small: {
        '& *': {
            fontSize: 12,
        },
    }
}));
export function Small(_, renderer, content) {
    const classes = this.theme.classes(SmallStyle);
    return renderer.create("div", { class: classes.small }, content);
}
//# sourceMappingURL=small.js.map