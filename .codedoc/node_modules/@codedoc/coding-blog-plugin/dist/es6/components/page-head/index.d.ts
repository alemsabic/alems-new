import { RendererLike } from '@connectv/html';
import { ThemedComponentThis } from '@connectv/jss-theme';
import { CodedocTheme } from '@codedoc/core';
export declare type PageHeadColorPreset = 'black' | 'white' | 'text' | 'background' | 'primary' | 'primary-contrast';
export declare function isPreset(color: string): color is PageHeadColorPreset;
export interface PageHeadOptions {
    lead?: string;
    color?: PageHeadColorPreset | string;
    shadow?: string;
    size?: string;
    leadSize?: string;
}
export declare function PageHead(this: ThemedComponentThis<CodedocTheme>, options: PageHeadOptions, renderer: RendererLike<any, any>, content: any): HTMLElement;
