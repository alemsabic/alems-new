import { themedStyle } from '@connectv/jss-theme';
export const SpaceStyle = themedStyle(theme => ({
    space: {
        '@media screen and (max-width: 1024px)': {
            '&[data-target="desktop"]': {
                display: 'none'
            }
        },
        '@media screen and (min-width: 1025px)': {
            '&[data-target="mobile"]': {
                display: 'none'
            }
        },
    }
}));
export function Space(options, renderer) {
    const classes = this.theme.classes(SpaceStyle);
    const opts = {};
    if (options.target)
        opts['data-target'] = options.target;
    return renderer.create("div", Object.assign({ class: classes.space, style: `height: ${options.space || '72px'}` }, opts));
}
//# sourceMappingURL=space.js.map