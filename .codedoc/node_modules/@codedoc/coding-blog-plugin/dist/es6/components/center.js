import { themedStyle } from '@connectv/jss-theme';
export const CenterStyle = themedStyle(theme => ({
    center: {
        textAlign: 'center'
    }
}));
export function Center(_, renderer, content) {
    const classes = this.theme.classes(CenterStyle);
    return renderer.create("div", { class: classes.center }, content);
}
//# sourceMappingURL=center.js.map