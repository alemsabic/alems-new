import { TagStyle } from './styles';
export function Tag(_, renderer, content) {
    const classes = this.theme.classes(TagStyle);
    const el$ = renderer.create("span", null, content);
    let tag = (el$.textContent || '').trim().replace(/\s/g, '_').replace(/[^\w-]/g, '');
    if (tag.startsWith('#'))
        tag = tag.substr(1);
    return renderer.create("a", { class: classes.tag, "data-page-tag": tag },
        renderer.create("code", null,
            renderer.create("span", { class: classes.indicator }, "#"),
            tag));
}
//# sourceMappingURL=index.js.map