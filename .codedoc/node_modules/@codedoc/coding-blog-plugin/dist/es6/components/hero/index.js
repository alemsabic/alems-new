import { HeroStyle } from './style';
export function Hero(options, renderer, content) {
    const classes = this.theme.classes(HeroStyle);
    const opts = { class: classes.hero };
    if (options.mode)
        opts['data-mode'] = options.mode;
    if (options.target)
        opts['data-target'] = options.target;
    if (options.leak)
        opts['style'] = `margin-bottom: -${options.leak}`;
    let img$ = renderer.create("img", { src: options.src, class: classes.image, "data-hero": true });
    if (options.target) {
        let media = '';
        if (options.target === 'desktop')
            media = '(min-width: 1025px)';
        else
            media = '(max-width: 1024px)';
        img$ = renderer.create("picture", null,
            renderer.create("source", { media: media, srcset: options.src }),
            renderer.create("img", { src: 'data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=', class: classes.image, "data-src": options.src, "data-hero": true }));
    }
    return renderer.create("div", Object.assign({}, opts),
        img$,
        renderer.create("span", { class: classes.caption }, content));
}
//# sourceMappingURL=index.js.map