import { themedStyle } from '@connectv/jss-theme';
export const BigStyle = themedStyle(theme => ({
    big: {
        '& *': {
            fontSize: 28,
        },
    }
}));
export function Big(_, renderer, content) {
    const classes = this.theme.classes(BigStyle);
    return renderer.create("div", { class: classes.big }, content);
}
//# sourceMappingURL=big.js.map