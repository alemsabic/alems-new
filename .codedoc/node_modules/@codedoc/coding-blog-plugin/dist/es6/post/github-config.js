import { StaticRenderer } from '@connectv/sdh';
export function addGitHubConfig() {
    const renderer = new StaticRenderer();
    return function (html, _, config) {
        var _a;
        if ((_a = config.misc) === null || _a === void 0 ? void 0 : _a.github) {
            renderer.render(renderer.create("script", null, `window.githubConfig=${JSON.stringify(config.misc.github)}`)).on(html.head);
        }
    };
}
//# sourceMappingURL=github-config.js.map