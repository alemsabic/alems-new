"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.addSource = void 0;
var sdh_1 = require("@connectv/sdh");
function addSource() {
    var renderer = new sdh_1.StaticRenderer();
    return function (html, file, config) {
        var source = {
            base: config.src.base,
            path: file.path,
            namespace: config.dest.namespace,
            title: config.page.title,
        };
        var script = renderer.create("script", null, "window.source = " + JSON.stringify(source));
        var container = html.body.querySelector('#-codedoc-container');
        if (container) {
            if (container.firstChild)
                renderer.render(script).before(container.firstChild);
            else
                renderer.render(script).on(container);
        }
        else {
            renderer.render(script).on(html.head);
        }
    };
}
exports.addSource = addSource;
//# sourceMappingURL=source.js.map