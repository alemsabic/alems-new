import { ConfigOverride } from '@codedoc/core';
import { PartialFeedConfig } from './feed';
export interface CodingBlogPluginOptions {
    assets?: string[];
    feed?: PartialFeedConfig;
}
export declare function codingBlog(options?: CodingBlogPluginOptions): () => ConfigOverride;
