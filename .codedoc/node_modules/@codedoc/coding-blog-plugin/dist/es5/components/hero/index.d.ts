import { RendererLike } from '@connectv/html';
import { ThemedComponentThis } from '@connectv/jss-theme';
import { CodedocTheme } from '@codedoc/core';
export interface HeroOptions {
    src: string;
    mode?: 'light' | 'dark';
    target?: 'mobile' | 'desktop';
    leak?: number;
}
export declare function Hero(this: ThemedComponentThis<CodedocTheme>, options: HeroOptions, renderer: RendererLike<any, any>, content: any): HTMLElement;
