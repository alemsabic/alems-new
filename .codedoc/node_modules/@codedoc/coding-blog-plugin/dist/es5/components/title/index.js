"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Title = exports.isPreset = void 0;
var style_1 = require("./style");
function isPreset(color) {
    return color === 'black' || color === 'white' || color === 'text' ||
        color === 'primary' || color === 'primary-contrast' || color === 'background';
}
exports.isPreset = isPreset;
function Title(options, renderer, content) {
    var classes = this.theme.classes(style_1.TitleStyle);
    var _color = undefined;
    var _class = undefined;
    if (options.color) {
        if (isPreset(options.color))
            _class = options.color;
        else
            _color = options.color;
    }
    return renderer.create("h1", { class: classes.h + " " + (_class || '') },
        renderer.create("span", { style: (_color ? "color: " + _color + ";" : '') + " \n      text-shadow: " + (options.shadow || 'none') + "; \n      font-size: " + (options.size || '48px') },
            options.lead ?
                renderer.create("span", { style: "font-size: " + (options.leadSize || '24px') + ";  margin-bottom: -48px; display: block" },
                    options.lead,
                    "\u00A0")
                : '',
            content));
}
exports.Title = Title;
//# sourceMappingURL=index.js.map