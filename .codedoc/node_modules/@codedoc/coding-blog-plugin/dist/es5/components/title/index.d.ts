import { RendererLike } from '@connectv/html';
import { ThemedComponentThis } from '@connectv/jss-theme';
import { CodedocTheme } from '@codedoc/core';
export declare type TitleColorPreset = 'black' | 'white' | 'text' | 'background' | 'primary' | 'primary-contrast';
export declare function isPreset(color: string): color is TitleColorPreset;
export interface TitleOptions {
    lead?: string;
    color?: TitleColorPreset | string;
    shadow?: string;
    size?: string;
    leadSize?: string;
}
export declare function Title(this: ThemedComponentThis<CodedocTheme>, options: TitleOptions, renderer: RendererLike<any, any>, content: any): HTMLElement;
