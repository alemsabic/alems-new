import { Item } from 'feed/src/typings';
import { Compiled } from '@connectv/sdh';
import { CodedocConfig } from '@codedoc/core';
import { File } from 'rxline/fs';
import { PartialConfig } from './config';
export declare function collect(conf: PartialConfig, add: (item: Item) => void): (html: HTMLDocument, file: File<Compiled>, config: CodedocConfig) => Promise<void>;
